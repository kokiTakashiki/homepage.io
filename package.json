{
  "name": "homepage.io",
  "version": "1.0.0",
  "description": "Personal homepage",
  "private": true,
  "scripts": {
    "format": "prettier --write . ; find . -name '*.html' -exec sed -i '' 's/<!doctype html>/<!DOCTYPE html>/g' {} +",
    "validate": "html-validate \"*.html\" \"mainContent/*.html\"",
    "validate:all": "html-validate --max-warnings=-1 \"*.html\" \"mainContent/*.html\""
  },
  "devDependencies": {
    "html-validate": "^9.5.3",
    "marked": "^17.0.2",
    "prettier": "^3.8.1"
  }
}
